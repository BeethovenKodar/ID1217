- Elevator application with dedicated controller and a GUI with buttons and showcase of the actual elevators in movement
- The GUI is written in java and was already provided as the `elevator.jar`
- The directory `c_src` is written from scratch apart from the hardware api files

**Compile and run**
start gui: java -Dfile.encoding=UTF-8 -jar ./elevator.jar -tcp -top 5 -number 3
start controller: cd c_src && make run
